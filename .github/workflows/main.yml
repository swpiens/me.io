name: build & deploy

on:
  push:
    branches: [ master ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Vuepress to GitHub Pages
      # You may pin to the exact commit or the version.
      # uses: testthedocs/github-pages-deploy-action@e88095834ea2719d0096dfa1be7692e0bdaeffa2
      uses: testthedocs/github-pages-deploy-action@0.0.3
